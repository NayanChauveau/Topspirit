{% extends 'base.html.twig' %}

{% block title %}Platform index{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
        {{ form_start(form) }}
            {{ form_row(form.submit, { 'label': 'Voter pour la plateforme : ' ~ platform.name }) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
