{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <div class="position-relative text-center ">
        <div class="col-md-5 p-lg-5 mx-auto my-5">
            <h1 class="display-4 fw-normal">Top-Spirit</h1>
            <p class="lead fw-normal">Tous les meilleurs sites de vente de vin et de spiritueux.</p>
            <a class="btn btn-outline-secondary" href="#">Voir le site du mois.</a>
        </div>
    </div>


    <div class="container mt-5">
        <div class="row">
        <div class="col-md-12">
            <div class="mb-4 shadow-sm border rounded position-relative" style="height: 250px; background-image: url('{{ vich_uploader_asset(activeUserAdd, 'imageFile') | imagine_filter('adv_banner') }}'); background-size: cover;">
                <a href="{{ activeUserAdd.advUrl }}" class="stretched-link"></a>
            </div>
        </div>
        {% for platform in platforms %}
            <div class="col-md-12">
                <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
                    <div class="col-auto d-none d-lg-block">
                        <img src="{{ vich_uploader_asset(platform, 'imageFile') | imagine_filter('miniature') }}" alt="{{ platform.bannerAlt }}">
                    </div>
                    <div class="col p-4 d-flex flex-column position-static">
                        <strong class="d-inline-block mb-2 text-primary">World</strong>
                        <h3 class="mb-0">{{ platform.name }}</h3>
                        <div class="mb-1 text-muted">Nov 12</div>
                        <p class="card-text mb-auto">{{ platform.content }}</p>
                        <a href="{{ platform.url }}" class="stretched-link">Visiter la plateforme</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    </div>
{% endblock %}
